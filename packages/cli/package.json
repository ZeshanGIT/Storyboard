{
  "name": "@storyboard/cli",
  "version": "0.1.0",
  "description": "Storyboard wireframe + product spec CLI",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ZeshanGIT/Storyboard.git",
    "directory": "packages/cli"
  },
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "storyboard": "./dist/bin/storyboard.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && cp -r src/templates dist/templates",
    "prepublishOnly": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "@storyboard/spec": "0.1.0",
    "@storyboard/shell": "0.1.0",
    "commander": "^14.0.0",
    "vite": "^8.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "~6.0.2",
    "vitest": "^3.2.6"
  }
}
