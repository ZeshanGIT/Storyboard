# Architecture

## P2 (this stub)

JSON product spec only. Preview wireframes with `onespec dev` from `todo-poc/`. No application runtime yet.

## P4 (planned)

TanStack Start with React 19, feature folders under `src/features/`, and tests traced to `BR-*` / `SR-*` ids. Stack conventions will be documented here when the cloud template ships.
